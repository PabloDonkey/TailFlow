# TailFlow AI Memory

- 2026-03-07T03:55:55.413Z - TailFlow stores repo-backed AI context under `.ai/`, separating directives in `.ai/rule.md` from dated, non-directive project memory in `.ai/memory.md`.
- 2026-03-07T03:55:55.413Z - TailFlow is split between `backend/` (FastAPI, SQLAlchemy 2, Pydantic v2) and `frontend/` (Vue 3, Vite, TypeScript, Pinia).
- 2026-03-07T03:55:55.413Z - The standard local workflow uses `make install` for setup and `make run` to start the backend and frontend together.
- 2026-03-07T03:55:55.413Z - Frontend API responses are validated with Zod schemas through a shared `fetchJSON` wrapper in `frontend/src/api/index.ts`.
- 2026-03-07T03:55:55.413Z - Pinia stores use composition-style stores with `loading` and `error` refs around async actions.
- 2026-03-07T03:55:55.413Z - Backend tests run against in-memory SQLite, and backend models favor SQLAlchemy's cross-database `sa.Uuid` type.
- 2026-03-11T04:02:16.618Z - Story 001 Phase 2 is implemented: `backend/app/services/tag_import.py` and `scripts/import_tags.py` import bundled `e621` and `booru` CSV catalogs into shared global tags, merging by name, filling `catalog_ids`, normalizing categories, and staying idempotent.
- 2026-03-11T04:02:16.618Z - Story 001 Phase 3 is implemented in the backend: dataset image tag assignments now reference shared `Tag` rows with ordered `position` and `is_protected`, trigger/class tags are enforced on upload and sync, and project metadata changes propagate to existing image assignments.
- 2026-03-11T04:02:16.618Z - VS Code/Pylance needs the repo root `pyrightconfig.json` so backend analysis resolves `backend/.venv` plus the `backend` import root for both `backend/` and `scripts/`.
- 2026-03-11T17:16:13.517Z - Story 001 Phase 4 backend tagging behavior is implemented: manual image tagging now respects `Project.tagging_mode`, shared user-defined tags remain available across modes, existing catalog-backed class tags are reused, protected trigger/class tags stay non-removable, and unknown manual tags require explicit confirmation before creation.
- 2026-03-11T19:50:00Z - Story 001 Phase 5 backend sync behavior is implemented: project sync now reads sibling sidecar `.txt` files, parses comma-separated tag names with trimming/deduping, adds only tags available in the active `tagging_mode` or shared user-defined tags, auto-creates unknown sidecar tags as shared tags, preserves existing manual tags, and keeps trigger/class assignments first.
- 2026-03-11T19:52:00Z - Story 001 Phase 6 frontend tagging UI is implemented: the project metadata screen can create and update `tagging_mode`, the image detail view renders protected trigger/class tags with source badges and disables their removal, and unknown manual tags prompt for confirmation before retrying creation with `create_missing`.
